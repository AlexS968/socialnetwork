image: maven:latest

stages:
  - build
  - deploy

build:
  stage: build
  tags:
    - docker
  script:
    - mvn clean package
    - cp target/Network-1.0-SNAPSHOT.jar /tmp/app.jar
  only:
    - 10-ci-cd

production:
  stage: deploy
  tags:
    - docker
  script:
    - java -jar /tmp/app.jar
  only:
    - 10-ci-cd
